# Discovered work

- Fix clear in-scope work inside the selected task.
- Use `bd todo add` for a small incidental follow-up and link it with `discovered-from`.
- Create a full task/bug for significant separate work.
- Use a nonblocking relation for context only.

Do not silently broaden scope, create a task for every reviewer observation, or make unrelated discoveries block the
current task.
