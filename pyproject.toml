[project]
name = "dstack"
version = "0.1.1"
requires-python = ">=3.12"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[project.optional-dependencies]
test = ["pytest>=8", "PyYAML>=6"]
