[project]
name = "dstack"
version = "0.4.3"
requires-python = ">=3.13"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests/fast"]

[dependency-groups]
dev = [
  "pytest>=8",
  "PyYAML>=6",
]
