# Operations

Normal operation starts from Beads:

```bash
bd prime --mcp
bd ready --json
```

Use the targeted dStack skills to plan, review, implement, and audit. Use
`dstack ctl` only for the deterministic mechanics called by those skills.
